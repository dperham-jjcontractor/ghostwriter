{
  "name": "draw_text",
  "description": "Type your whole reply onto the page, below the handwritten notes, using a slow virtual keyboard (about 100 characters per second). Plain ASCII text only; use line breaks for structure.",
  "internal_command": "draw_text",
  "parameters": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "The complete reply to type. Plain ASCII only, no markdown, under 700 characters."
      }
    },
    "required": ["text"]
  }
}
